# DontReloadMe
A simple demonstration page that blocks browser refresh actions and displays two digital-style clocks. The page shows the exact time it was loaded and a running real-time clock. It also includes a code sample on the page that documents the refresh-blocking script.
